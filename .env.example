# Socket.io realtime server (in-memory, no database)
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001

# Set to "true" to run the app with a fully client-side simulated room
# (no socket server needed). Handy for quick UI/UX iteration.
NEXT_PUBLIC_DEMO_MODE=false
