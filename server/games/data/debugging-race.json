{
  "kind": "quiz",
  "prompts": [
    {
      "text": "const x = 5; console.log(x.toUpperCase()); — what breaks?",
      "options": [
        "x is undefined",
        "x is a number — toUpperCase doesn't exist on it",
        "console.log is misspelled",
        "Missing semicolon"
      ],
      "answer": 1
    },
    {
      "text": "for (let i = 0; i < 10; i--) — what's wrong?",
      "options": [
        "10 should be 9",
        "i-- should be i++",
        "i is undeclared",
        "< should be >"
      ],
      "answer": 1
    },
    {
      "text": "if (x = 5) { … } — what's wrong?",
      "options": [
        "Assignment instead of comparison",
        "Nothing — it's fine",
        "x is undeclared",
        "Missing braces"
      ],
      "answer": 0
    },
    {
      "text": "array[array.length] — what's the problem?",
      "options": [
        "Index is one past the end",
        "Should be array.length()",
        "array is empty",
        "length is 0"
      ],
      "answer": 0
    },
    {
      "text": "function sum(a, b) { return a - b; } — for adding, what's wrong?",
      "options": [
        "Extra argument",
        "Missing return",
        "Returns the difference instead of the sum",
        "Wrong parameter names"
      ],
      "answer": 2
    },
    {
      "text": "SELECT * FORM users — what's wrong?",
      "options": [
        "users doesn't exist",
        "Missing semicolon",
        "SELECT is wrong",
        "FORM should be FROM"
      ],
      "answer": 3
    },
    {
      "text": "const name = 'Ada'; name = 'Grace'; — what's wrong?",
      "options": [
        "name is a reserved word",
        "Strings must use double quotes",
        "Cannot reassign a const",
        "Missing semicolon"
      ],
      "answer": 2
    },
    {
      "text": "let arr = [1, 2, 3]; arr[-1] — what's the result?",
      "options": [
        "undefined — negative index isn't valid here",
        "A syntax error",
        "arr is null",
        "3 — the last element"
      ],
      "answer": 0
    },
    {
      "text": "while (true) { } with no break — what happens?",
      "options": [
        "Missing semicolon",
        "Nothing runs",
        "Infinite loop",
        "Syntax error"
      ],
      "answer": 2
    },
    {
      "text": "if (user.name) { welcome(); } when user can be null — what's the risk?",
      "options": [
        "name is misspelled",
        "Braces are wrong",
        "welcome is undefined",
        "Null pointer — user could be null"
      ],
      "answer": 3
    },
    {
      "text": "const isEven = (n) => n % 2 === 1; — what's wrong?",
      "options": [
        "n is a string",
        "Arrow syntax is wrong",
        "=== should be ==",
        "Should be n % 2 === 0 for even"
      ],
      "answer": 3
    },
    {
      "text": "docker run -p 8080:80 nginx — but 'port is already in use'. Why?",
      "options": [
        "nginx is misspelled",
        "latest tag is invalid",
        "Docker isn't installed",
        "Something else already listens on 8080"
      ],
      "answer": 3
    },
    {
      "text": "const url = 'https://api.example.com/v1/user'; but the endpoint is '/users'.",
      "options": [
        "Wrong endpoint path",
        "Missing auth header",
        "CORS is blocking it",
        "http instead of https"
      ],
      "answer": 0
    },
    {
      "text": "function greet(name) { return 'Hi ' + name; } greet(); — what prints?",
      "options": [
        "An error",
        "Hi ",
        "Hi null",
        "Hi undefined"
      ],
      "answer": 3
    },
    {
      "text": "const x = 10; const y = 0; console.log(x / y); — what prints in JS?",
      "options": [
        "null",
        "Infinity",
        "An error",
        "0"
      ],
      "answer": 1
    }
  ]
}
