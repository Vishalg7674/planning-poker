{
  "kind": "quiz",
  "prompts": [
    {
      "text": "Which query returns every row from the 'users' table?",
      "options": [
        "SELECT users FROM *",
        "SELECT * FROM users",
        "SELECT * users",
        "FROM users SELECT *"
      ],
      "answer": 1
    },
    {
      "text": "Which query returns users older than 30?",
      "options": [
        "SELECT * FROM users WHERE age > 30",
        "SELECT * FROM users WHERE age >= 30",
        "SELECT age FROM users WHERE age > 30",
        "SELECT * FROM users HAVING age > 30"
      ],
      "answer": 0
    },
    {
      "text": "Which query counts the rows in 'orders'?",
      "options": [
        "SELECT SUM(*) FROM orders",
        "SELECT LEN(*) FROM orders",
        "SELECT COUNT(*) FROM orders",
        "SELECT TOTAL(*) FROM orders"
      ],
      "answer": 2
    },
    {
      "text": "Which clause filters groups after aggregation?",
      "options": [
        "ORDER BY",
        "GROUP BY",
        "HAVING",
        "WHERE"
      ],
      "answer": 2
    },
    {
      "text": "Which query sorts users by name, descending?",
      "options": [
        "SELECT * FROM users ORDER BY name DESC",
        "SELECT * FROM users GROUP BY name DESC",
        "SELECT * FROM users ORDER BY name ASC",
        "SELECT * FROM users SORT BY name DESC"
      ],
      "answer": 0
    },
    {
      "text": "Which query returns only distinct cities?",
      "options": [
        "SELECT DISTINCT city FROM users",
        "SELECT UNIQUE city FROM users",
        "SELECT city FROM users DISTINCT",
        "SELECT * FROM users GROUP BY city"
      ],
      "answer": 0
    },
    {
      "text": "Which query joins 'orders' to 'users' on user_id?",
      "options": [
        "SELECT * FROM orders JOIN users ON orders.user_id = users.id",
        "SELECT * FROM orders LEFT JOIN users ON users.user_id = orders.id",
        "SELECT * FROM orders, users WHERE id = user_id",
        "SELECT * FROM orders JOIN users"
      ],
      "answer": 0
    },
    {
      "text": "Which query changes a user's name to 'Ada'?",
      "options": [
        "ALTER users SET name = 'Ada'",
        "MODIFY users SET name = 'Ada'",
        "UPDATE users SET name = 'Ada' WHERE id = 1",
        "INSERT INTO users (name) VALUES ('Ada')"
      ],
      "answer": 2
    },
    {
      "text": "Which query deletes only the user with id 1?",
      "options": [
        "DELETE FROM users",
        "REMOVE users WHERE id = 1",
        "DELETE FROM users WHERE id = 1",
        "DROP FROM users WHERE id = 1"
      ],
      "answer": 2
    },
    {
      "text": "Which query returns the highest age?",
      "options": [
        "SELECT HIGHEST(age) FROM users",
        "SELECT TOP(age) FROM users",
        "SELECT MAX(age) FROM users",
        "SELECT GREATEST(age) FROM users"
      ],
      "answer": 2
    },
    {
      "text": "Which query counts users per country?",
      "options": [
        "SELECT country FROM users GROUP BY name",
        "SELECT country, COUNT(*) FROM users GROUP BY country",
        "SELECT * FROM users GROUP BY country",
        "SELECT country, COUNT(*) FROM users ORDER BY country"
      ],
      "answer": 1
    },
    {
      "text": "Which query inserts a brand-new user?",
      "options": [
        "INSERT INTO users (name) VALUES ('Ada')",
        "CREATE users (name) 'Ada'",
        "ADD ROW users (name) = 'Ada'",
        "UPDATE users SET (name) = 'Ada'"
      ],
      "answer": 0
    },
    {
      "text": "Which query returns the average age of users?",
      "options": [
        "SELECT MEDIAN(age) FROM users",
        "SELECT MID(age) FROM users",
        "SELECT AVG(age) FROM users",
        "SELECT MEAN(age) FROM users"
      ],
      "answer": 2
    },
    {
      "text": "Which query returns users whose name starts with 'A'?",
      "options": [
        "SELECT * FROM users WHERE name STARTS 'A'",
        "SELECT * FROM users WHERE name IN 'A'",
        "SELECT * FROM users WHERE name = 'A'",
        "SELECT * FROM users WHERE name LIKE 'A%'"
      ],
      "answer": 3
    },
    {
      "text": "Which query removes the whole 'users' table?",
      "options": [
        "REMOVE TABLE users",
        "DROP TABLE users",
        "TRUNCATE TABLE users",
        "DELETE TABLE users"
      ],
      "answer": 1
    }
  ]
}
